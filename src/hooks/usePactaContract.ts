/**
 * 与 Pacta 合约相关的链上读接口（与 usePactaChainReads 相同，便于按「合约 hook」命名发现）。
 */
export { usePactaChainReads as usePactaContractReads } from "./usePactaChain";
export type { OnChainPact } from "./usePactaChain";
